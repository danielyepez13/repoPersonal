/**
 * Factory para crear instancias de queries.
 * Centraliza la creación de queries para facilitar el testing y la consistencia.
 * 
 * Nota: Con la migración a GraphQL, los datos se obtienen directamente
 * del cliente GraphQL en los loaders de SvelteKit (+page.ts, +page.ts[id]).
 * Este archivo se mantiene para futuras extensiones.
 */
export class QueryFactory {
    // Métodos de factory pueden agregarse aquí según sea necesario
}
